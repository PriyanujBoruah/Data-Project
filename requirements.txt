Flask>=2.0
pandas>=1.3
numpy>=1.20
scikit-learn>=0.24 # For normalization
scipy>=1.7       # For outlier detection (Z-score)
chardet>=4.0     # To detect encoding (optional but helpful)
werkzeug>=2.0    # Ensure compatible version